// command_controller.h
// Accepts commands from the serial port and executes them, using the appropriate controller.

#pragma once

// Reads and executes the next incomming command from the Mission Boss.
void read_and_execute_next_incoming_command();

